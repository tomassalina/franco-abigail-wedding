import { LogoIcon } from "@/components/icons";

export const Header = () => {
  return (
    <header className="flex items-center justify-center">
      <LogoIcon height={50} className="h-10 md:h-[50px]" />
    </header>
  );
};
