import { LogoIcon } from "@/components/icons";

export const Header = () => {
  return (
    <header className="bg-accent flex items-center justify-center py-5 px-4">
      <LogoIcon height={50} />
    </header>
  );
};
